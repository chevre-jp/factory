import { IExtendId } from '../../autoGenerated';
import { IClientUser } from '../../clientUser';
import { IAccount, IAttributes as IMoneyTransferActionAttributes } from '../action/moneyTransfer';
import * as TransactionFactory from '../transaction';
import { AccountTransactionType } from '../transactionType';

export type IRecipient = TransactionFactory.IRecipient;
export type IAgent = TransactionFactory.IAgent;

export type IStartParamsWithoutDetail
    = TransactionFactory.IStartParams<AccountTransactionType.Transfer, IAgent, IRecipient, IObjectWithoutDetail>;

/**
 * 取引開始パラメーターインターフェース
 */
export type IStartParams = TransactionFactory.IStartParams<AccountTransactionType.Transfer, IAgent, IRecipient, IObject>;

export type IResult = any;

/**
 * エラーインターフェース
 */
export type IError = any;

export interface IObjectWithoutDetail {
    clientUser?: IClientUser;
    amount: {
        value: number;
    };
    fromLocation: TransactionFactory.ISimpleAccount;
    toLocation: TransactionFactory.ISimpleAccount;
    description?: string;
}

/**
 * 取引対象物インターフェース
 */
export interface IObject {
    clientUser?: IClientUser;
    /**
     * 金額
     */
    amount: {
        value: number;
    };
    /**
     * 転送元
     */
    fromLocation: IAccount;
    /**
     * 転送先
     */
    toLocation: IAccount;
    description?: string;
}

export interface IPotentialActions {
    moneyTransfer: IMoneyTransferActionAttributes;
}

export type ITransaction = IExtendId<IAttributes>;

/**
 * 転送取引インターフェース
 */
export interface IAttributes extends TransactionFactory.IAttributes<IStartParams, IResult, IError, IPotentialActions> {
}
